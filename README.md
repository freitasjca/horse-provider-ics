# horse-provider-ics
OverbyteICS transport provider for the Horse framework — a drop-in alternative to Indy with modern OpenSSL 3.x/4.x TLS (TLS 1.3, mTLS). Delphi/Windows; enabled by {$DEFINE HORSE_PROVIDER_ICS}.
